# ScrapeGraph-specific additions to the corpus service requirements.
# Resolved and pinned during the build/test phase (see requirements.txt, which is the installed set):
#   PyPI's newest scrapegraph-py line is 1.47.x; no 2.x release exists yet, so the '>=2.1.0' target of the
#   amendment cannot be satisfied. The SDK surface is isolated in scraper/extractors/scrapegraph_managed.py.
scrapegraph-py==1.47.0,<2
# Self-hosted/private extraction is optional (requirements-local-ai.txt):
# scrapegraphai==1.76.0
